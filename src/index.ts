//import { XSwipe } from "./xswipe";

export * from "./xswipe";
export * from "./types";

/*let swipee = new XSwipe({
    name: "XSwipe",
    wrapper: "[data-xswipe]",
    elements: "a",
    onCloseClick: () => {
        console.log("Close button clicked!");
    },
    opts: {
        bgColor: "rgba(0, 0, 0, 0.9)",
        zoomLevel: 1,
        allowZoom: true,
        allowFullscreen: true,
    },
});*/
